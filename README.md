# prompt-formatter-mcp
MCP server for optimizing and formatting prompts into structured formats (Markdown, XML, JSON) to enhance LLM performance across different models

## Features
- 🎯 Convert prompts to Markdown, XML, or JSON formats
- 🚀 Optimize prompt structure for better LLM performance
- 🔄 Adapt prompts for different models (Claude, GPT, etc.)
